import styled from 'styled-components';

interface ContentProps {}

export function Content(props: ContentProps) {
  return <StyledContent>asdsad</StyledContent>;
}

// styled
const StyledContent = styled.div`
  padding: 1em;
`;
