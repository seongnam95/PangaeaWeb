import styled from 'styled-components';

interface StateGroupProps {}

export function StateGroup(props: StateGroupProps) {
  return <StyledStateGroup></StyledStateGroup>;
}

// styled
const StyledStateGroup = styled.div``;
