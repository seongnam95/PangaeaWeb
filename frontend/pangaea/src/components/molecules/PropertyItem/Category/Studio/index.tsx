import styled from 'styled-components';

export interface StudioType {}

export function Studio(props: StudioType) {
  return <StyledStudio></StyledStudio>;
}

// styled
const StyledStudio = styled.div``;
